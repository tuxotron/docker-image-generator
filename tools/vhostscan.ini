name = vhostscan
command = git clone https://github.com/codingo/VHostScan.git /opt/VHostScan && \
          cd /opt/VHostScan && \
          pip3 install numpy pandas && \
          python3 setup.py install
category = scanner
status = disabled
