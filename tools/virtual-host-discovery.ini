name = virtual-host-discovery
command = apt install -y ruby-dev && \
          git clone https://github.com/AlexisAhmed/virtual-host-discovery.git && \
          cd virtual-host-discovery && \
          chmod +x scan.rb && \
          ln -sf /opt/virtual-host-discovery/scan.rb /usr/local/bin/virtual-host-discovery.rb
category = recon
