name = cloudflair
command = git clone https://github.com/christophetd/CloudFlair.git /opt/CloudFlair && \
            cd /opt/CloudFlair && \
            pip3 install -r requirements.txt && \
            ln -sf /opt/CloudFlair/cloudflair.py /usr/local/bin/cloudflair.py
category = recon
