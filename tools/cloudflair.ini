name = cloudflair
command = apt install -y python3 python3-pip && \
            git clone https://github.com/christophetd/CloudFlair.git && \
            cd CloudFlair && \
            pip3 install -r requirements.txt && \
            ln -sf /CloudFlair/cloudflair.py /usr/local/bin/cloudflair.py
category = recon
