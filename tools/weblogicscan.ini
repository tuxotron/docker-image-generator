name = weblogicscan
command = git clone https://github.com/rabbitmask/WeblogicScan.git /opt/weblogicscan && \
          cd /opt/weblogicscan && \
          pip3 install -r requirements.txt
category = scanner
