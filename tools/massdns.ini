name = massdns
command = apt install -y build-essential && git clone https://github.com/blechschmidt/massdns.git /opt/massdns && \
          cd /opt/massdns/ && \
          make && \
          ln -sf /opt/massdns/bin/massdns /usr/local/bin/massdns
category = recon

