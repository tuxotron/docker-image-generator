name = massdns
command = apt install -y build-essential && git clone https://github.com/blechschmidt/massdns.git && \
              cd massdns/ && \
              make && \
              ln -sf /massdns/bin/massdns /usr/local/bin/massdns
category = recon

