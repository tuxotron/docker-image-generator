name = gopherus
command = apt install -y python2 && \
          curl https://bootstrap.pypa.io/get-pip.py --output get-pip.py && \
          python2 get-pip.py && \
          git clone https://github.com/tarunkant/Gopherus.git /opt/Gopherus && \
          cd /opt/Gopherus && \
          chmod +x install.sh && \
          ./install.sh
category = exploit
