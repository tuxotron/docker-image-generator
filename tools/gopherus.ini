name = gopherus
command = apt install -y python python-pip && \
          git clone https://github.com/tarunkant/Gopherus.git && \
          cd Gopherus && \
          chmod +x install.sh && \
          ./install.sh
category = exploit
