name = bucket_finder
command = apt install -y ruby-dev && \
    git clone https://github.com/AlexisAhmed/bucket_finder.git && \
    cd bucket_finder && \
    chmod +x bucket_finder.rb && \
    ln -sf /bucket_finder/bucket_finder.rb /usr/local/bin/bucket_finder.rb
category = recon
