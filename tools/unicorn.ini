name = unicorn
command = git clone https://github.com/trustedsec/unicorn.git /opt/unicorn && \
            cd /opt/unicorn && \
            ln -sf /opt/unicorn/unicorn.py /usr/local/bin/unicorn
category = powershell
