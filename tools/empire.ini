name = empire
command = apt install -y python python-pip sudo && \
          git clone https://github.com/EmpireProject/Empire.git && \
          cd Empire && \
          ./setup/install.sh
category = exploit
