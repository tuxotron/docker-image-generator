name = empire
command = apt install -y python python-pip python-dev lsb-core apt-utils sudo && \
          git clone --depth=1 -b dev https://github.com/EmpireProject/Empire.git && \
          cd Empire/setup && \
          pip install pefile && \
          ./install.sh && \
          rm -rf /opt/Empire/data/empire* && \
          STAGING_KEY=RANDOM python setup_database.py
category=exploit