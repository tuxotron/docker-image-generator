name = anonsurf
command = apt install -y iptables && \
        git clone https://github.com/Und3rf10w/kali-anonsurf.git /opt/kali-anonsurf && \
        cd /opt/kali-anonsurf && ./installer.sh
category = misc
comment = You need to run the container in privileged mode